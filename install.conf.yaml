- defaults:
    link:
      create: true
      relink: true

- link:
    ~/.terminfo: terminfo
    ~/.config/fish: fish
    ~/.config/nvim: nvim
    ~/.config/wezterm: wezterm
    ~/.config/marksman: marksman
    ~/.config/starship.toml: starship.toml

- clean:
    ~/.config:
      recursive: true

- create:
    ~/dev:
    ~/dev/bin:
    ~/dev/tmp:
    ~/dev/work:
    ~/dev/tools:
    ~/dev/projects:

- shell: []
